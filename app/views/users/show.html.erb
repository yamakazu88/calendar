<div class="mypage">
  <div class="posts-list">
    <p><%= @nickname %>さんの投稿一覧</p>
    <% if @posts.any? %>
      <table>
        <thead>
          <tr>
            <th>タイトル</th>
            <th>時間</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td><%= post.title %></td>
              <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
              <td><%= link_to '詳細', post %></td>
              <td><%= link_to '編集', edit_post_path(post.id) %></td>
              <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      まだ投稿がありません
    <% end %>
  </div>

  <%= week_calendar events: @posts do |date, posts| %>
    <%= date.day %>
    <% if posts.any? %>
      <% posts.each do |post| %>
        <div>
          <%= link_to post.title, post %>
        </div>
      <% end %>
    <% end %>
  <% end %>


  <div class="category-area">
    <div class="category-list">
      <p><%= @category1.name %>の投稿一覧</p>
      <% if @post1.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post1.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category2.name %>の投稿一覧</p>
      <% if @post2.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post2.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category3.name %>の投稿一覧</p>
      <% if @post3.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post3.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category4.name %>の投稿一覧</p>
      <% if @post4.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post4.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category5.name %>の投稿一覧</p>
      <% if @post5.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post5.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category6.name %>の投稿一覧</p>
      <% if @post6.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post6.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category7.name %>の投稿一覧</p>
      <% if @post7.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post7.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

    <div class="category-list">
      <p><%= @category8.name %>の投稿一覧</p>
      <% if @post8.any? %>
        <table>
          <thead>
            <tr>
              <th>タイトル</th>
              <th>時間</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @post8.each do |post| %>
              <tr>
                <td><%= post.title %></td>
                <td><%= post.start_time.strftime("%Y-%m-%d %H:%M") %></td>
                <td><%= link_to '詳細', post %></td>
                <td><%= link_to '編集', edit_post_path(post.id) %></td>
                <td><%= link_to '削除',post_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        まだ投稿がありません
      <% end %>
    </div>

  </div>
</div>